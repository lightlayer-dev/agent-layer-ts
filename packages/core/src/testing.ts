/**
 * Test utilities for agent-layer packages.
 *
 * Import from "@agent-layer/core/testing" — not included in the production bundle.
 */
export {
  makeJwt,
  validJwtPayload,
  baseIdentityConfig,
  testRoutes,
  testMcpConfig,
  makeCustomVerifier,
} from "./test-utils.js";
